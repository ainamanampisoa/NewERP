@model List<NewERP.Models.SalarySlip>
@{
    ViewData["Title"] = "Tableau des employés";
    Layout = "_Layout";
}

@section Styles {
    <link href="~/css/fournisseurs.css" rel="stylesheet" />
}

<div class="fournisseurs-container">
    <h1 class="fournisseurs-title">
        <i class="fas fa-hand-holding-usd"></i>Tableau de salaire des employés
    </h1>
    <form method="get" asp-controller="Employe" asp-action="Tableau" class="fournisseurs-form">
        <div class="form-grid">
            <div class="form-group">
                <label>Mois</label>
                <select id="mois" name="mois" class="fournisseurs-select">
                    <option value="">-- Tous les mois --</option>
                    <option value="1">Janvier</option>
                    <option value="2">Février</option>
                    <option value="3">Mars</option>
                    <option value="4">Avril</option>
                    <option value="5">Mai</option>
                    <option value="6">Juin</option>
                    <option value="7">Juillet</option>
                    <option value="8">Août</option>
                    <option value="9">Septembre</option>
                    <option value="10">Octobre</option>
                    <option value="11">Novembre</option>
                    <option value="12">Décembre</option>
                </select>
            </div>

            
            @*<div class="form-group">
                <label>Année</label>
                <select id="annee" name="annee" class="fournisseurs-select">
                    <option value="">-- Toutes les années --</option>
                    @for (int an = 2022; an <= 2026; an++)
                    {
                        <option value="@an">@an</option>
                    }
                </select>
            </div>*@

            <div class="form-group button-container">
                <button class="fournisseurs-button primary compact">
                    <i class="fas fa-square-check"></i></i> Valider
                </button>
            </div>
        </div>
    </form>

    @if (Model != null && Model.Any())
    {   
        <div class="total-summary">
            <div class="summary-section">
                <h3 class="summary-title">Total général</h3>
                <div class="summary-item">
                    <i class="fas fa-wallet"></i>
                    <span>Earnings:</span>
                    <strong>@(((decimal)ViewBag.TotalEarnings).ToString("N0")) $</strong>
                </div>
                <div class="summary-item">
                    <i class="fas fa-minus-circle"></i>
                    <span>Deductions:</span>
                    <strong>@(((decimal)ViewBag.TotalDeductions).ToString("N0")) $</strong>
                </div>
                <div class="summary-item">
                    <i class="fas fa-dollar-sign"></i>
                    <span>Salaire net:</span>
                    <strong>@(((decimal)ViewBag.TotalNetPay).ToString("N0")) $</strong>
                </div>
            </div>

            <div class="summary-section">
                <h3 class="summary-title">Earnings</h3>
                @if (ViewBag.TotalByComponent != null)
                {
                    foreach (var component in (Dictionary<string, decimal>)ViewBag.TotalByComponent)
                    {
                        <div class="summary-item">
                            <i class="fas fa-dollar-sign"></i>
                            <span>@component.Key:</span>
                            <strong>@component.Value.ToString("N0") $</strong>
                        </div>
                    }
                }
            </div>

            <div class="summary-section">
                <h3 class="summary-title">Deductions</h3>
                @if (ViewBag.TotalByDec != null)
                {
                    foreach (var component in (Dictionary<string, decimal>)ViewBag.TotalByDec)
                    {
                        <div class="summary-item">
                            <i class="fas fa-dollar-sign"></i>
                            <span>@component.Key:</span>
                            <strong>@component.Value.ToString("N0") $</strong>
                        </div>
                    }
                }
            </div>
        </div>

        <div class="devis-container">
            @*<table class="devis-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Nom</th>
                        <th>Statut</th>
                        <th>Brut</th>
                        <th>Total Deduction</th>
                        <th>Salaire net</th>
                        <th>Earning</th>
                        <th>Mois</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var slip in Model)
                    {
                        <tr>
                            <td>@slip.Name</td>
                            <td>@slip.EmployeeName</td>
                            <td><span class="status-badge success">@slip.Status</span></td>
                            <td>@slip.GrossPay.ToString("N0") $</td>
                            <td>@slip.TotalDeduction.ToString("N0") $</td>
                            <td><strong>@slip.NetPay.ToString("N0") $</strong></td>
                            <td>
                                @if (slip.Earnings != null && slip.Earnings.Any())
                                {
                                    <ul style="list-style-type:none; padding-left:0; margin:0;">
                                        @foreach (var earning in slip.Earnings)
                                        {
                                            <li>
                                                <strong>@earning.SalaryComponent</strong>: @earning.Amount.ToString("N0") $
                                            </li>
                                        }
                                    </ul>
                                }
                                else
                                {
                                    <span>Aucun earning</span>
                                }
                            </td>
                            <td>@slip.StartDate.ToString("MMMM yyyy")</td>
                        </tr>
                    }
                </tbody>
            </table>*@
            <table class="devis-table">
                <thead>
                    <tr>
                        @*<th>ID</th>*@
                        <th>Nom</th>
                        <th>Statut</th>
                        @foreach (var component in (List<string>)ViewBag.EarningComponents)
                        {
                            <th>@component</th>
                        }
                        @foreach (var component in (List<string>)ViewBag.DeductionComponents)
                        {
                            <th>@component</th>
                        }
                        <th>Brut</th>
                        <th>Total Déduction</th>
                        <th>Salaire Net</th>
                        <th>Mois</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var slip in Model)
                    {
                        <tr>
                            @*<td>@slip.Name</td>*@
                            <td>@slip.EmployeeName</td>
                            <td>
                                <span class="status-badge success">@slip.Status</span>
                            </td>
                            @foreach (var component in (List<string>)ViewBag.EarningComponents)
                            {
                                var earning = slip.Earnings?.FirstOrDefault(e => e.SalaryComponent == component);
                                <td>
                                    @if (earning != null)
                                    {
                                        @earning.Amount.ToString("N0") <text>$</text>
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </td>
                            }

                                      
                            @* Afficher les valeurs pour chaque composant de déductions *@
                            @foreach (var component in (List<string>)ViewBag.DeductionComponents)
                            {
                                var earning = slip.Deductions?.FirstOrDefault(e => e.SalaryComponent == component);
                                <td>
                                    @if (earning != null)
                                    {
                                        @earning.Amount.ToString("N0") <text>$</text>
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </td>
                            }
                            <td>@slip.GrossPay.ToString("N0") $</td>
                            <td>@slip.TotalDeduction.ToString("N0") $</td>
                            <td><strong>@slip.NetPay.ToString("N0") $</strong></td>
                            <td>@slip.StartDate.ToString("MMMM yyyy")</td>
                        </tr>
                    }
                </tbody>
            </table>

        </div>
    }
    else
    {
        <div class="slipty-state">
            <i class="fas fa-user-slash"></i>
            <h3>Aucun salaire trouvé</h3>
            <p>Aucun salaire ne correspond à vos critères de recherche</p>
        </div>
    }
    <div class="pagination">
        <ul class="pagination-list">
            @for (int i = 1; i <= (int)ViewBag.TotalPages; i++)
            {
                if (i == (int)ViewBag.CurrentPage)
                {
                    <li class="active"><span>@i</span></li>
                }
                else
                {
                    <li>
                        <a asp-action="Tableau"
                        asp-route-page="@i"
                        asp-route-pageSize="@ViewBag.PageSize"
                        asp-route-mois="@ViewBag.Mois"
                        asp-route-annee="@ViewBag.Annee">@i</a>
                    </li>
                }
            }
        </ul>
    </div>
</div>


